{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs for the waiting-screen bridge",
  "windows": ["main"],
  "permissions": ["core:default"]
}
